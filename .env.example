# Copy to server/.env. START-HERE.bat creates this automatically.
DATABASE_URL="postgresql://bnk:bnk_password@localhost:5432/bahay_ni_kuya_db?schema=public"

# Express API port
PORT=4000

# Secret used to sign auth (JWT) tokens - change this to any long random string
JWT_SECRET="change-me-to-a-long-random-string"
